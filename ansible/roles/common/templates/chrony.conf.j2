# Managed by Ansible - Overseer
{% for pool in ntp_pools | default([]) %}
pool {{ pool }} iburst
{% endfor %}
{% for server in ntp_servers | default([]) %}
server {{ server }} iburst
{% endfor %}

driftfile /var/lib/chrony/drift
makestep 1.0 3
rtcsync
logdir /var/log/chrony
